# 本次验证记录

环境：Windows / Node.js 22.22.0，2026-09-05。

2026-09-06 城市自动规划更新：

- 河网 v2：用带抖动的稀疏节点、不同方向的连接、曲线河段和变化宽度代替固定 1100 米平行重复。新规划 v3 的桥位和地块禁建判定使用同一河网；旧区域边界清单冻结在 hydrology-v2 元数据，外围以 280 米过渡带衔接。
- 原 28 项测试通过，新增 2 项河网确定性、多方向、非周期复制、旧区保留和地块校验测试通过；构建成功。「自然河网」浏览器定位 141 / 159，可见弯曲河道和桥梁，无脚本错误。
- 此版本是程序化河道布局，不是按真实降雨和流量计算的水文模拟。旧区域仍保留原河道，水面局部物理仍沿用原实现。

- 弯路试验 v2：未保存区域使用连续内部变形生成弧线与错位路口，方正地块避让道路并重新连接入口。已保存 v1 区域不改动，区域周界仍使用 v1 道路接口；尚未改成丁字路网。新增确定性、曲率、旧区边界兼容测试通过。
- 浏览器「弯路新区」定位 68 / 68，周围 9 个区域均为 v2，画面可见弧形道路，未捕获脚本错误。道路避让与护岸保护会减少这一试验版本的可领取地块数量。

- `npm test` 27 项通过，`npm run build` 成功。新增确定性与负/远坐标生成、共享道路边界、城区坡度、规划保存与重启一致性、跨区地块间隔、旧领地保护、桥面朝向及行走高度测试。
- 浏览器进入新区 32 / 32 后，自动返回周围 3×3 规划区域、121 块地皮和 84 段去重道路。可定位沿街地块，第一人称按 W 后位置为 (2310, 2327)，地面高度 4.598 米、视点高 6.298 米；未捕获脚本错误。
- 道路与地面重叠闪烁已修复；在河床低于 0 米的位置，桥面碰撞保持在公共道路高度。普通城区坡度目标不包含局部下切护岸和保留的历史高地基。
- 升级前备份于 work/town-before-city.sqlite。现有 2 / 0 地块仍归 demo-owner，地基 12.3 米，咖啡屋发布 ID 未变。新规划保存在 city_regions，旧地基保存在 city_legacy，未重算旧地基。
- 第一版城市道路仍以有宽度变化的规则街区为主，规划区域不等于玩家地皮。全局地形/道路规则升级仍需兼容版本或专门迁移；数学无限容量、实时玩家同步和真实 GitHub 部署不在此次验证范围。

- 官网轻互动：按钮改为圆角，概念图支持地形、建筑、地块独立开关与恢复，桌面鼠标轻微倾斜，卡片悬停和滚动进入反馈。浏览器验证隐藏 6 栋建筑、恢复图层、手机关闭地块层正常；390 像素无溢出，无脚本错误。减少动态效果模式下变换清空、过渡为 0 秒。

- openworld 品牌更新：官网使用黑白配色、等宽标识和线框世界插画，游戏界面同步名称及中性面板。GitHub 入口链接至站内开源计划，明确为仓库筹备中，未指向虚构仓库。
- 项目与本机数据迁至 `C:/Users/TamikiP/Documents/ChatGPT/openworld`，新目录运行服务。构建及 21 项测试通过；1440 像素桌面、390 像素手机检查正常，无横向溢出或脚本错误，开源入口正常定位。

- 官网更新：根路径为独立展示页，游戏入口为 `/game`，OAuth 回跳同步到游戏。21 项测试通过，API 测试覆盖展示页不加载游戏脚本、游戏路由及未配置 OAuth 的回跳。
- 浏览器验证：1440 像素桌面与 390 像素手机布局，手机无横向溢出；FAQ 展开、官网进入游戏、游戏品牌链接返回官网均通过，未捕获脚本错误。首页未请求 `/app.js`。

- 64 米地块更新：当前 `npm test` 共 21 项通过，构建成功。新增完整 64×64 米地基采样、6 米间隔、64×24×64 米模型通过、超宽/超深/超高拒绝，以及旧领地迁移保持归属和发布引用、重复启动不重复迁移的验证。
- 本机数据库已备份，原 2 / 0 领地主人与咖啡屋发布 ID 均保留；地基重算为 12.3 米。运行中的 API 已返回宽深 64 米、限高 24 米、中心间距 70 米。

- `npm test`：18 项通过，覆盖地块独占、河道禁建、地基高度、审核状态转换、CSRF/管理员权限、私有模型访问、GLB 大小/尺寸/变换/外链限制、PNG 解码和贴图尺寸限制，以及 v2 缓坡和远坐标延展、水波传播衰减、干岸阻挡与步长稳定性。
- 水面浏览器实测：通过「河水互动」跳转 6 / 0 河道，点击可触发水波扰动事件；白昼和 23:00 夜间画面正常，未捕获脚本或着色器错误。反射更新按渲染成本自适应限频；自动化浏览器操作曾出现等待帧稳定超时，未给出帧率保证。
- v2 地形在测试采样范围内最高海拔小于 38 米，采样坡度梯度小于 0.4；验证 ±2000000000 地块坐标有效。地形网格跨区复用，旧区释放。
- 已备份本机 v1 数据库并执行 v2 迁移，逐项比较升级前后的地块坐标、主人与已发布模型 ID，结果一致。
- 固定地形测试：中心空地、河谷与山丘、跨区连续性、远坐标回归采样及服务器端地块适建判定。
- 浏览器实测：本地演示账号登录 → 领取 2 / 0 地块 → 上传咖啡屋 → 管理员旋转预览 → 批准 → 退出登录后访客看到 1 栋公开建筑。
- 浏览器实测：跳转 -5 / 5 自然坡地、昼夜切换；390×844 手机视口未产生水平溢出，未捕获脚本错误。
- 浏览器实测：第一人称按 W 后 Z 从 48 变为 47.25；地面海拔 4 米，视点正常为 5.7 米，跳跃达到 6.5 米，Esc 释放鼠标并返回地图。
- `npm run build` 成功；`npm audit --omit=dev --audit-level=high` 报告 0 漏洞。Sharp 已升级至 0.35.4。

验证边界：尚未配置真实 GitHub OAuth App、生产域名或远程服务器，未验证真实 GitHub 授权登录，也未实际构建/启动 Docker 容器。当前 URL 是本机演示服务；本地演示数据不包含在交付 ZIP 中。生产使用全新持久卷并按 README 配置。
